server:
  port: ${PORT:8086}

spring:
  datasource:
    url: jdbc:${DATABASE_URL}/${DB_NAME:borrow_db}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true